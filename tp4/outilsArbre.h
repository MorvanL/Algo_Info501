#include "Arbre.h"
#include "pile.h"

void afficherArbreTrieRecursif (noeud*);
void afficherArbreIteratif (noeud*);
int compterArbre (noeud*, int*);
noeud* minimumArbre (noeud*);
void transplanter (noeud**, noeud*, noeud*);
